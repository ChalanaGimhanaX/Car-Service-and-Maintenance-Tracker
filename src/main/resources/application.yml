spring:
  application:
    name: car-service-maintenance-tracker
  datasource:
    url: ${DB_URL:jdbc:mysql://mysql-2113e934-spxmedlolme-d121.c.aivencloud.com:27184/defaultdb?sslMode=REQUIRED&serverTimezone=UTC}
    username: ${DB_USERNAME:avnadmin}
    password: ${DB_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  thymeleaf:
    cache: false
  flyway:
    enabled: true

server:
  port: 8080

logging:
  level:
    org.springframework.security: info
    org.hibernate.SQL: debug
